# DeepCalm AI — 2026-05-11 交接清单

## 背景

启动 Boss Mode 管线，主线是 GSC 修复 + 留言板找回 + 页脚优化。

***

## 1. CC0 音频下载（P1）

- **birds-nature.mp3** — 从 Pixabay 下载 13.63MB CC0 音效
- 存到 `public/audio/birds-nature.mp3`
- 绕过 Cloudflare 两次（先用浏览器 UA，再用 JSON-LD contentUrl）
- 放弃 Freesound.org（需 API key 登录）

## 2. 热线移除（P2）

- **问题**：7 个语言字典的 12 处热线号码引用
- **修复**：全部替换为「建议找当地心理诊所」的通用提示
- 涉及文件：zh.json, en.json, ms.json, ja.json, ko.json, th.json, es.json
- 每字典约 12 处替换

## 3. GSC 重定向修复（核心修复，共 5 项）

### 3a. middleware.ts 爬虫检测

- **问题**：Googlebot 访问 `/{lang}/xxx` 时，若 cookie 有不同语言值，会被 302 跳转到 cookie 语言版本
- **修复**：在 LOCALE\_PATTERN 分支内加入 crawler UA 检测
- 关键代码：`/Googlebot|bingbot|Baiduspider|YandexBot/i.test(ua)` → `return NextResponse.next()`

### 3b. layout.tsx 移除 hreflang 冲突

- **问题**：layout 的 generateMetadata 始终返回 `/${lang}/`（根路径），所有子页面继承错误 hreflang
- **修复**：从 layout 完全移除 alternates，各页面自己管理

### 3c. anxiety/\[slug] 补全 7 语言 alternates

- **之前**：只有 zh/en/ms 3 语言
- **修复**：补全 zh/en/ms/ja/ko/th/es，加 canonical

### 3d. guide/\[slug] 添加完整 metadata

- **之前**：完全缺失 alternates、canonical、metadataBase
- **修复**：全套添加（7 语言 alternates + canonical + metadataBase）

### 3e. topic/\[slug] 添加完整 metadata

- **之前**：完全缺失 alternates、canonical、metadataBase
- **修复**：全套添加（7 语言 alternates + canonical + metadataBase）

## 4. 留言板 ResonanceWall（P2）

- **要求**：「那块卡片，是让每个人可以上去留言心情的看板」
- **组件**：`src/components/ResonanceWall.tsx`（136 行）
  - 10 条英文 + 10 条中文种子消息
  - 每 7 秒自动推送一条种子
  - 20 秒淡出，22.5 秒移除
  - 用户输入 + 发送按钮
  - globalCount 随机 500-2000
- **嵌入位置**：SanctuaryPage 的 SeoContent 和 `</main>` 之间

## 5. 页脚 SitemapFooter（P2）

- **组件**：`src/components/SitemapFooter.tsx`（68 行）
- 5 列 grid：Sleep / Anxiety / Grief / Loneliness / Self-worth / Relationships / Identity / Mindfulness / Emotional Health
- **嵌入页面**：
  - anxiety/\[slug] — 替换内联 footer
  - topic/\[slug] — 替换内联 footer
- SanctuaryPage 原本就有 SitemapFooter

## 6. 构建与部署（P3）

- **npm run build**：212/212 页面，零错误通过 ✅
- **Vercel 部署**：`vercel deploy -y --no-wait` 成功
- **预览地址**：`https://deepcalm-91oma1b62-youeshopent-8741s-projects.vercel.app`
- **Git remote**：origin 已删除（原为假地址），以后用 `vercel deploy --prod -y` 手动部署

## 7. 已知问题 & 待办

- [ ] GSC 验证：已在 GSC 点击「验证修复」，等 Google 重新抓取
- [ ] 留言板：用户上线验收
- [ ] 生产域名绑定：如需 deepcalm-ai.com 生产部署，另行安排

