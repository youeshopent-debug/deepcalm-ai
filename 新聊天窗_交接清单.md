# DeepCalm AI 部署就绪 · 交接清单

## 完成状态

- [x] **环境自检** — package.json / next.config.js 均符合 Vercel 要求
- [x] **构建验证** — `npm run build` 零错误通过，4/4 静态页生成
- [x] **SEO 词数** — EN 527 字 / ZH 1006 字符（均 ≥500/800 目标）
- [x] **.env.example** — 已写好 OPENAI_API_KEY / SILICONFLOW_API_KEY 模板
- [x] **Vercel CLI** — 已安装 v52.0.0（无需再装）

## 构建输出

```
✓ Compiled successfully
✓ Linting and checking validity of types
✓ Collecting page data
✓ Generating static pages (4/4)
✓ Collecting build traces
✓ Finalizing page optimization

Route (app)            Size       First Load JS
┌ ○ /                  6.72 kB    104 kB
└ ○ /_not-found        873 B      88.1 kB
```

## 下一步（你需操作）

### 1. GitHub 推送（未操作）
```bash
git remote add origin git@github.com:你的用户名/deepcalm-ai.git
git branch -M main
git push -u origin main
```

### 2. Vercel 部署
```bash
vercel login          # 浏览器登录
vercel --prod         # 一键部署
```

### 3. 申请 API Key 后填 .env.local
```env
OPENAI_API_KEY=sk-xxx
SILICONFLOW_API_KEY=sf-xxx
```

### 4. Vercel 设置环境变量
Vercel Dashboard → Project → Settings → Environment Variables  
添加 `OPENAI_API_KEY` 和 `SILICONFLOW_API_KEY`

## 文件结构（关键）
```
deepcalm-ai/
├── public/locales/     # zh.json / en.json / ms.json
├── src/
│   ├── app/            # layout.tsx + page.tsx（SSG）
│   ├── components/     # 9 个组件（Hero/Header/AiCounselor/...）
│   ├── lib/            # sleep.ts / mockCounselor.ts / i18n.ts
│   ├── context/        # LanguageContext.tsx
│   └── types/          # index.ts
├── .env.example        # 环境变量模板
├── next.config.js      # trailingSlash: true
└── package.json
```
