# DeepCalm AI · 视觉设计需求交接书

> **交付对象**：美杜莎 (UI/UX 设计师)
> **优先级**：⚡ 紧急 — 阻塞开发
> **项目**：DeepCalm AI — 深夜情绪避难所
> **对接链路**：美杜莎（设计）→ 李长寿（前端 Tailwind 实现）

---

## 1. 设计目标与品牌调性

**核心定位**：*"深夜 11 点到凌晨 3 点的数字避难所"*

| 维度 | 描述 |
|------|------|
| 情绪 | 安睡 · 包裹感 · 治愈 · 温柔 |
| 视觉 | 微光 · 流体 · 朦胧 · 呼吸 |
| 用户画像 | 27-35 岁都市失眠人群、焦虑型脑力工作者 |

**设计 KPI**：首屏停留时长 **+200%**，60 秒交互完成率 **≥85%**

---

## 2. 色彩系统

### 2.1 主色基底（占 70% 版面）

| Token | 色值 | 用途 |
|-------|------|------|
| `--dc-bg` | `#0F1420` | 页面背景（废弃 `nord-bg: #2E3440`） |
| `--dc-surface` | `#1A2238` | 卡片/面板底色 |
| `--dc-elevated` | `#242B45` | hover/选中态 |
| `--dc-border` | `#2D3561` | 分割线/边框 |

### 2.2 柔光字体（占 25%）

| Token | 色值 | 透明度 |
|-------|------|--------|
| `--dc-text-primary` | `#E8E9F3` | 92% |
| `--dc-text-secondary` | `#A8ACC4` | 70% |
| `--dc-text-muted` | `#6B6F8A` | 50% |
| `--dc-text-softglow` | `#F0F0FF` | 100% + blur(1px) |

### 2.3 极光渐变（占 5% — 核心视觉锤）

```
--dc-aurora-gradient: linear-gradient(135deg,
  #4A6CF7 0%,     /* 靛蓝紫 */
  #6C5CE7 25%,    /* 紫 */
  #A29BFE 45%,    /* 淡紫 */
  #81ECE3 65%,    /* 极光青 */
  #55C595 85%,    /* 薄荷绿 */
  #4A6CF7 100%    /* 闭环回靛蓝 */
);
```

**动效要求**：`background-position` 左右平移，周期 6-8 秒，模拟极光流动。

### 2.4 语义点缀色

| 语义 | 色值 | 用途 |
|------|------|------|
| 安慰蓝 | `#4A90D9` | 呼吸引导/正向反馈 |
| 柔绿 | `#55C595` | 完成打卡 |
| 暖琥珀 | `#E8B87A` | 晚间提醒 |
| 月光金 | `#F0D68A` | AI 特殊回复高亮 |

---

## 3. 呼吸感 Hero 组件

```
┌─────────────────────────────────────┐
│   ◕  ← 呼吸光晕圆（极光渐变）        │
│   "DeepCalm" ← font-weight: 200      │
│   "请跟我一起呼吸"                    │
│   [ 开始 ] [ 5分钟 ] [ 10分钟 ]      │
│   ↓ "向下滑动进入避难所"             │
└─────────────────────────────────────┘
```

**呼吸光晕圆技术规范**：

| 属性 | 值 |
|------|-----|
| 尺寸 | 移动端 120px → 桌面端 200px |
| 缩放 | `scale(1.0)` ↔ `scale(1.25)` |
| 周期 | 吸入 4s → 屏息 1.5s → 呼出 4s → 屏息 1.5s |
| 光晕 | `box-shadow: 0 0 60px 20px rgba(74,108,247,0.3)` |
| 曲率 | `cubic-bezier(0.4, 0, 0.2, 1)` |
| 交互 | 点击圆 → 暂停呼吸 → 触发"心率匹配"模式 |

**背景层**：`#0F1420` + 模糊 120px 的 `radial-gradient` 粒子噪点（纯 CSS 实现，不用图片）。

---

## 4. Glassmorphism 组件规范

### 4.1 统一参数

```css
background: rgba(26, 34, 56, 0.65);
backdrop-filter: blur(20px) saturate(1.1);
border: 1px solid rgba(255, 255, 255, 0.06);
border-radius: 16px;
box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
```

### 4.2 环境音按钮（AmbientAudio）

```
┌─────────────────┐
│  🔊 篝火  3:45  │  玻璃质感卡片
│  ▂▃▄▆██▆▄▃▂    │  canvas 极光渐变频率柱
│  [▶]  [❤ 234]   │  播放/收藏
└─────────────────┘
```

- 播放按钮 48×48px，播放态外圈呼吸光晕
- 收藏激活态 `#E87A7A`
- 移动端水平滑动，桌面端网格

### 4.3 AI 咨询卡片（AiCounselor）

```
       圆脸图标
   "我是 DeepCalm 心灵伙伴"
  ┌────────────────────────┐
  │ 用户气泡 → 右对齐      │  20px 4px 20px 20px
  │ ← AI 气泡 左对齐       │  4px 20px 20px 20px
  └────────────────────────┘
  [ 输入你的感受... ] [→]
```

- 用户气泡：`rgba(74,108,247,0.25)`，右对齐
- AI 气泡：`rgba(42,50,82,0.6)`，左对齐
- 输入框焦点态 → 极光渐变 `1px solid`

---

## 5. 共鸣墙（ResonanceWall）— 流星动效

### 核心概念
情绪记录以 **流星姿态** 在墙上流动划过，而非静态卡片堆叠。

```
  ✦ "今晚的月光像你的拥抱"  ← 淡入 → 水平划过 → 淡出
       ✧ "焦虑终于走远了"    ← 延迟 1.2s 后划过
  ✧ "3点17分，我决定原谅自己" ← 尾部拖尾光效
```

| 属性 | 规格 |
|------|------|
| 入场 | `opacity: 0→1` + `translateX(60px)` |
| 移动 | 水平滚动 30-45s/全宽（随机 Y 偏移） |
| 消散 | opacity 渐隐 + scale 缩至 0.8 |
| 尾迹 | 线性渐变左→右透明度递减 |
| 并发上限 | 6-10 条（超出回收最旧） |
| 触发 | 滚动进入视口自动播放 |
| 技术 | `requestAnimationFrame` + CSS `translate3d` 硬件加速 |
| 移动端降级 | 并发降至 4 条，关闭拖尾光效 |

---

## 6. 字体系统

| 层级 | 大小 | 字重 | 字体 |
|------|------|------|------|
| Display XL | `clamp(2.5rem,5vw,4rem)` | 200 | Inter / Noto Sans SC |
| H1 | `clamp(1.5rem,3vw,2.5rem)` | 300 | (同上) |
| H2 | `clamp(1.25rem,2vw,1.75rem)` | 400 | (同上) |
| Body | `clamp(0.875rem,1.5vw,1rem)` | 400 | (同上) |

纯 Google Fonts 开源，无商业字体风险。

---

## 7. 响应式断点

| 断点 | 宽度 | Hero 圆 | 网格 |
|------|------|---------|------|
| Mobile | < 640px | 120px | 单列 |
| Tablet | 640-1023px | 160px | 双列 |
| Desktop | ≥ 1024px | 200px | 三列 |

间距：4px 基准（4/8/12/16/20/24/32/48/64）

---

## 8. 优先级排期

| 阶段 | 内容 | 工时 | 阻塞 |
|------|------|------|------|
| **P0** | 色彩系统 + Hero 组件 | 10h | ✅ 阻塞开发 |
| **P1** | Glassmorphism 组件库 + 共鸣墙动效 | 14h | ❌ |
| **P2** | AI 气泡 + 环境音播放器 | 10h | ❌ |
| **P3** | 53 页面批量套用 | 16h | ❌ |

**交付节奏**：Day 1 晚 P0 → Day 2 晚 P1 → Day 3 晚 P2 → Day 4 晚验收

---

## 9. 合规红线

- 字体：仅 Inter / Noto Sans SC（Google Fonts）
- 图标：Heroicons / Lucide（开源）
- 素材：纯 CSS 实现，零外部图片
- 无障碍：WCAG 2.1 AA（对比度 ≥ 4.5:1）
- i18n：文案预留配置位，英文默认语言

---

**设计交付路径**：Figma Design Token JSON → 李长寿接入 `tailwind.config.ts`

**美杜莎，这是完整的设计需求。色彩系统已定义到 CSS 变量级别，呼吸 Hero 的参数可直接用于 Figma 原型复刻。P0 的 10 小时（色彩系统 + Hero）是开发阻塞项，建议今晚优先出稿。**
